
# MOOG:
arreglar la creación de dispositivos -> no se si habra alguna manera de eliminarlos al cerrar el script
    -> hacer la liista de dispositivos
    -> ver como crear sin tener que reiniciar sd
        -> una idea es usar archivos .py en vez de jupyter, pero tendria que buscar una manera comoda de lanzarlos
hacer un notebook nuevo (audio.ipynb) donde no usemos el callback de sound sino que se conecte al dispositivo out desde pactl.py

# LANZADOR:
crear un fichero plantilla con todos los imports y la estructura basica
hacer un script que lance el ultimo y abra una copia de la plantilla para editar el siguiente

tambien podria tener un jupyter donde ir creando cada modulo
y un script que obtenga de cada celda del juoter el codigo y lo meta en el main de una plantilla y lo ejecute
1. funcion bucle que detecte un archivo en una carpeta temporal y lo ejecute
2. cada celda es una funcion basada en una plantilla en la que declara el modulo 
3. al ejecutar la celda, se genera el archivo py en la carpeta temporal, tambien se genera otra celda con la plantilla vacia
4. el bucle detecta el archivo y lo ejecuta

TODO ver si puedo evitar tener que meter los modulos en el registro manualmente
TODO (muy a futuro) ver como puedo simplificar lo de helvium

# FILTROS:
check kS y los filtros
hacer un high pass filter
hacer un filtro usando fourier

# SECUENCIADOR - MIDI:
en helvium tambien se puede controlar el MIDI
deberia hacer una clase que mande Una señal cada negra (reloj)
y clases que reciban de ese reloj
TODO investigar todos los mensajes MIDI que puedo usar


# UI
podria hacer una UI externa para controlar la clase Const
de hecho si hago otra para controlar los ins y los outs, me quito de tener que usar helvium

checkear qtnodes y NodeGraphQt como alternativa a helvium, (aunque lo mismo es más rápido un fork de helvium).
En nodegraphqt se pueden poner sliders y esas cosas, lo cual sería super práctico

En algun momento podría traducir las cosas al ingles :p

Reorganizar y dejar un notebook con ejemplos y tests y otro para experimentar
